var assert = require('assert');
var assert = require('chai').assert;
describe('Array', function() {
  describe('#indexOf()', function() {
    it('second test version', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});